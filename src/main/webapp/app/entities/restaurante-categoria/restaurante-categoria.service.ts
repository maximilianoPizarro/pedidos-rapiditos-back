import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IRestauranteCategoria } from '@/shared/model/restaurante-categoria.model';

const baseApiUrl = 'api/restaurante-categorias';

export default class RestauranteCategoriaService {
  public find(id: number): Promise<IRestauranteCategoria> {
    return new Promise<IRestauranteCategoria>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IRestauranteCategoria): Promise<IRestauranteCategoria> {
    return new Promise<IRestauranteCategoria>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IRestauranteCategoria): Promise<IRestauranteCategoria> {
    return new Promise<IRestauranteCategoria>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IRestauranteCategoria): Promise<IRestauranteCategoria> {
    return new Promise<IRestauranteCategoria>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
